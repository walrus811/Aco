const { describe, it } = require("mocha");
const { assert } = require("chai");

describe("index", function () {
  it("test", function () {
    assert.equal(true, true, "can't be occurred");
  });
});
